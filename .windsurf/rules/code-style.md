---
trigger: always_on
---

all functions should use camelCase
use camelCase for variables
use snake_case for constants
use camelCase for methods
use CamelCase for classes
use php 8 style attributes for tests
always run syntax check
always run code formatter
always use braces on if statements
string concatination using . should have spaces on both sides
using '!' for not should not have a space after it
@param in docblocks should use one space before and after the type
use parentheses for instantiating new classes
ensure all code is PSR12 compliant from the beginning
set a line length limit of 120
use phpcs, phpcbf, and php-cs-fixer for linting
for pint use config .pint.json in the project root dir
