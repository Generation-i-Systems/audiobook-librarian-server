help me build planning document for an android app that consumes an openapi spec
from https://books.thelin.org/api-docs/openapi.json to download and play audiobooks.
there should be a browse mode that lets the user browse books by recent adds or
drill down by genre,author, series, title with lists of each that add more filters
as you go, or by searching for a book matching any of those criteria. It should
use google signin with the login also using the backend so the user is signed
into the backend system once signed in. The name of the app is Audiobook
Librarian. The package name is org.thelin.librarian. The app should use modern
android styling and features. It should also be built with as much KMP as possible
to facilitate a later iOS integration. It should show book covers in various sizes
on different screens. The covers will come from the backend. In book book find mode
there should be a button bar at the bottom to switch finding methods or to list
the locally downloaded books. Books will download in a zip file and should be
unzipped on download into a directory that is specified by the user. Audiobooks
in the root of the public storage by default. With the offer to create it if
it does not already exist. The player will show the cover downloaded from the
backend with the book along with standard player controls including a "chapter"
next and previous which will work to jump from one file to the another if
there are multiple audio files for the book or jump between chapters for
m4b files which have chapter data included. Playback speed should be adjustable.
The progress for the book and the total book time should be displayed and
should be adjusted according to the book speed. there should be manual
bookmarks and auto bookmarks. Auto bookmarks get added everytime the
book is paused or time jumped. The app should keep statistics on listening
times and books finished. Which may be sent back to the server in a future update.


