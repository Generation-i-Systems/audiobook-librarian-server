# Legacy Documentation

These documents are kept for historical reference only. They describe build-time plans, one-off
migration/fix session summaries, or point-in-time status reports (test coverage, PHPStan cleanup,
tech-stack modernization, etc.). They are **not maintained** and may describe commands, services,
or architecture (e.g. MongoDB/Firestore document stores) that no longer exist in this codebase.

Do not use these as setup or usage instructions. See the top-level [README](../../README.md) and
the current guides in [`docs/`](../) instead.
